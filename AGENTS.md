# 协作执行规则

- 修改前先阅读根目录 `README.md` 和当前子项目的 `README.md`。
- `data/raw/` 是官方原始资料，只读；不要重命名、覆盖或在其中生成缓存。
- 不得提交 `研赛内部资料`、付费资料、重复压缩包、凭据或嵌套 `.git`。
- 公共数据口径、坐标转换、能耗、通信覆盖和调度工具放入 `src/common/`，避免四个项目复制实现。
- Q1、Q2、Q3、Q4 通过 `schemas/` 中定义的机器可读文件交接，禁止依赖未记录的 notebook 状态。
- 所有距离、时间、质量、能量和功率字段必须在名称或 schema 中明确单位。
- 启发式或随机算法必须固定并记录随机种子；输出同时保留目标值与可行性校验结果。
- 不修改官方提交模板结构；最终写表逻辑独立放置并进行列名、行数和空值检查。

