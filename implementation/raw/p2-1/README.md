# P2-1：可行路线集合与资源排程

运行 `python solve_p2_1.py`。程序先按服务区和地理邻近关系构造、合并可行多站路线，再用 `q2_common` 的逐段载荷能耗、时间窗、无人机和共享电池离散事件模拟器排程。若初始路线导致医疗/首批硬时限违约，则自动启动路线集合修复搜索，选择可行候选继续输出。

所有结果写入 `outputs/`，图形写入 `figures/`，不修改项目原有文件。
