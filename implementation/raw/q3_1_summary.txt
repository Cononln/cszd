问题三 · 方法1：NSGA-II（全局分组编码 + 通信/中继联合调度）—— 结果汇总
============================================================

零、关键建模假设清单（附录3未给出可执行文字算法/公式时的合理默认，非题目原文）
  1. 路径损耗用标准自由空间公式 FSPL=32.44+20log10(d_km)+20log10(f_MHz)。
  2. 地形遮挡按DEM直线遮挡二元判定加10dB(Lobs)，非渐进式菲涅尔区计算。
  3. 中继仅单跳(运输<->中继<->G01)——数据里没有中继<->中继参数，这是数据结构决定的结论，不是假设。
  4. 爬升/下降段视为在起讫点水平位置原地垂直升降，巡航段定高直线飞行——与q1_1/q2_common能耗时间模型的既有假设一致。
  5. 中继悬停位置离散化为'缺口腿水平连线5个分数点 x 6档悬停高度(50-300m)'的候选网格，非连续优化。
  6. 相位内任一采样点被遮挡即判定整个相位(爬升/巡航/下降)全程需要中继覆盖，偏保守但避免子相位区间追踪。
  7. 中继能耗用'功率 x 时长'模型，区别于运输的Lg(q)航程包线模型——因为附件给中继的是功率参数而非航程包线字段。
  8. 单条路线的中继需求最多归并为2个(位置,时间窗)——对应2架中继机身的物理上限；超出诚实标记为违反。
  9. 每次中继任务视为'O01起飞->到位服务->飞回O01'的完整架次，不做跨悬停点的连续repositioning。

种群/代数：pilot pop=24 gen=5（平均每代0.54s）；正式 pop=80 gen=80，实际总耗时672.6s
通信/中继全局预计算：需中继相位=586/720，中继候选点=3420个，零候选硬缺口相位=0个，预计算用时=679.7s(含GA全程，命中磁盘缓存时该值主要是GA耗时)

一、覆盖校验：送达80/80箱（分组编码结构性保证100%覆盖，无需额外的集合划分约束；通信不可行只记violation,不剔除路线）

二、代表解（前沿中架次数最少、能耗次优先的可行解；若前沿无可行解则取违反量最小者）
  f1(及时性,加权迟到量)=7487083.0
  f2(makespan)=54352.2 s (15.10 h)  [运输=53828.0s  中继=54352.2s]
  f3(总能耗)=135.639 kWh  [运输=110.856kWh  中继=24.782kWh]
  f4(架次数)=90（其中多停靠架次8个，占比14.5%）  [运输架次=55  中继架次=35]
  硬约束违反数=12

三、通信保障方式分布：55条路线中 直连=20  中继覆盖=35  中继缺口=0

四、与q2版本（不建模通信/中继）对照
  q2_1(NSGA-II全局解)：架次数=29，总能耗=82.777kWh，硬约束违反数=0
  q2_2(贪心基线)   ：架次数=16，总能耗=58.179kWh，硬约束违反数=5
  q3_1(本方案)     ：架次数=90(运输55+中继35)，总能耗=135.639kWh(运输110.856+中继24.782)，硬约束违反数=12
  q3_1的总能耗/架次数均应不低于q2_1的对应运输分量——这是符合预期的结果：叠加通信保障后
  必须额外派遣中继架次、消耗额外能量，是约束变严格带来的合理代价，不是回归或bug。

五、范围裁剪说明
  单架次最多3个停靠服务区；派单规则固定为'紧迫覆盖优先+应急优先系数密度'，不参与搜索；
  中继悬停位置为离散候选网格搜索结果，非连续优化(见建模假设5)。